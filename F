


pdfmetrics.registerFont(TTFont('Arabic', '29ltbukraregular.ttf'))

# إعداد ملف PDF
doc = SimpleDocTemplate("Arabic_Text.pdf", pagesize=A4)
styles = getSampleStyleSheet()
arabic_style = ParagraphStyle('Arabic', parent=styles['Normal'], fontName='Arabic', fontSize=14, leading=20, rightIndent=0, alignment=2)

# النص العربي
text = "تمارين نفسية وفق منهج كارل يونج"

# إعادة تشكيل النص وضبط اتجاهه
reshaped_text = arabic_reshaper.reshape(text)
bidi_text = get_display(reshaped_text)

# إنشاء الفقرة وإضافتها إلى المستند
story = []
story.append(Paragraph(bidi_text, arabic_style))
story.append(Spacer(1, 12))

# بناء ملف PDF
doc.build(story)
